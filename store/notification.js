export const state = () => ({
  items: []
})

export const getters = {
  getNotifications: (state) => state.items,
  notificationsAmount: (state) => 0
}

export const mutations = {}

export const actions = {}
